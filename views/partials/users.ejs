<h3>All My Friends</h3>
<ul>
  <% data.forEach(function(d) { %>
    <li data-id=<%=d.id%>>
      <input  class="input_item" type="text" name="first" 
        placeholder="<%= d.first %>" readonly >
      <span class="edit">edit</span>
      <span class="delete">delete</span>
    </li> 
   <% }) %>
</ul>
